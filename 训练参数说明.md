# 3D Gaussian Splatting 训练参数完整说明

## 目录
- [模型参数](#模型参数-modelparams)
- [优化参数](#优化参数-optimizationparams)
- [渲染管线参数](#渲染管线参数-pipelineparams)
- [使用示例](#使用示例)
- [参数调优建议](#参数调优建议)

---

## 模型参数 (ModelParams)

这些参数控制数据加载和模型基础配置。

| 参数 | 简写 | 默认值 | 类型 | 说明 |
|------|------|--------|------|------|
| `--source_path` | `-s` | "" | str | **[必需]** 数据集路径，包含 COLMAP 或 NeRF 格式数据 |
| `--model_path` | `-m` | "" | str | **[必需]** 模型输出路径，保存训练结果 |
| `--sh_degree` | - | 3 | int | 球谐函数阶数（0-3），控制视角相关颜色的复杂度 |
| `--images` | - | "images" | str | 图像文件夹名称（相对于 source_path） |
| `--depths` | - | "" | str | 深度图文件夹路径（可选，用于深度正则化） |
| `--resolution` | `-r` | -1 | int | 图像分辨率缩放：<br>-1 = 自动<br>1 = 全分辨率<br>2 = 1/2 分辨率<br>4 = 1/4 分辨率 |
| `--white_background` | `-w` | False | bool | 使用白色背景（用于合成数据集，如 Blender） |
| `--train_test_exp` | - | False | bool | 训练集和测试集分别使用曝光补偿 |
| `--data_device` | - | "cuda" | str | 数据存储设备（"cuda" 或 "cpu"）<br>大数据集使用 "cpu" 可节省显存 |
| `--eval` | - | False | bool | 评估模式（加载测试集） |

---

## 优化参数 (OptimizationParams)

这些参数控制训练过程和优化策略。

### 基础训练参数

| 参数 | 默认值 | 类型 | 说明 |
|------|--------|------|------|
| `--iterations` | 30,000 | int | 总训练迭代次数 |
| `--optimizer_type` | "default" | str | 优化器类型：<br>"default" = 标准 Adam<br>"sparse_adam" = 稀疏 Adam（更快，需安装扩展） |

### 学习率参数

#### 位置学习率（指数衰减）
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--position_lr_init` | 0.00016 | 位置初始学习率 |
| `--position_lr_final` | 0.0000016 | 位置最终学习率（衰减到此值） |
| `--position_lr_delay_mult` | 0.01 | 位置学习率延迟乘数 |
| `--position_lr_max_steps` | 30,000 | 位置学习率衰减的总步数 |

#### 其他属性学习率（固定）
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--feature_lr` | 0.0025 | 特征（颜色/球谐系数）学习率 |
| `--opacity_lr` | 0.025 | 不透明度学习率 |
| `--scaling_lr` | 0.005 | 缩放参数学习率 |
| `--rotation_lr` | 0.001 | 旋转参数学习率 |

#### 曝光补偿学习率
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--exposure_lr_init` | 0.01 | 曝光补偿初始学习率 |
| `--exposure_lr_final` | 0.001 | 曝光补偿最终学习率 |
| `--exposure_lr_delay_steps` | 0 | 曝光学习率延迟步数 |
| `--exposure_lr_delay_mult` | 0.0 | 曝光学习率延迟乘数 |

### 密集化控制参数

密集化是 3DGS 的核心机制，通过克隆和分裂高斯来增加场景细节。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--densify_from_iter` | 500 | 开始密集化的迭代次数 |
| `--densify_until_iter` | 15,000 | 停止密集化的迭代次数 |
| `--densification_interval` | 100 | 密集化间隔（每 N 次迭代执行一次） |
| `--densify_grad_threshold` | 0.0002 | 密集化梯度阈值<br>梯度超过此值的高斯会被克隆或分裂 |
| `--percent_dense` | 0.01 | 密集化百分比阈值（场景范围的 1%） |
| `--opacity_reset_interval` | 3,000 | 不透明度重置间隔<br>定期将接近透明的高斯重置，避免过早剔除 |

### 损失函数参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--lambda_dssim` | 0.2 | SSIM 损失权重<br>总损失 = (1-λ)×L1 + λ×(1-SSIM)<br>默认：0.8×L1 + 0.2×SSIM |
| `--depth_l1_weight_init` | 1.0 | 深度 L1 损失初始权重 |
| `--depth_l1_weight_final` | 0.01 | 深度 L1 损失最终权重（指数衰减） |
| `--random_background` | False | 训练时使用随机背景颜色（数据增强） |

---

## 渲染管线参数 (PipelineParams)

这些参数控制渲染过程的实现细节。

| 参数 | 默认值 | 类型 | 说明 |
|------|--------|------|------|
| `--convert_SHs_python` | False | bool | 在 Python 中计算球谐转 RGB（vs CUDA）<br>调试用，会降低速度 |
| `--compute_cov3D_python` | False | bool | 在 Python 中计算 3D 协方差（vs CUDA）<br>调试用，会降低速度 |
| `--debug` | False | bool | 启用调试模式，生成调试文件 |
| `--antialiasing` | False | bool | 启用抗锯齿（减少高频区域的锯齿） |

---

## 使用示例

### 基础训练
```bash
# 最简单的训练命令
python train.py -s data/my_scene -m output/my_scene
```

### 高质量训练
```bash
# 更多迭代次数，获得更好的质量
python train.py -s data/scene -m output/scene \
    --iterations 50000 \
    --densify_until_iter 20000
```

### 低显存训练
```bash
# 将数据放在 CPU 上，节省 GPU 显存
python train.py -s data/scene -m output/scene \
    --data_device cpu \
    -r 2  # 同时降低分辨率
```

### 使用深度正则化
```bash
# 需要预先准备深度图
python train.py -s data/scene -m output/scene \
    --depths depth_maps \
    --depth_l1_weight_init 1.0
```

### 启用高级特性
```bash
# 曝光补偿 + 抗锯齿
python train.py -s data/scene -m output/scene \
    --train_test_exp \
    --antialiasing
```

### 快速训练（稀疏 Adam）
```bash
# 需要安装 diff-gaussian-rasterization[3dgs_accel]
python train.py -s data/scene -m output/scene \
    --optimizer_type sparse_adam
```

### 合成数据集训练
```bash
# Blender 等合成数据，使用白色背景
python train.py -s data/blender_scene -m output/blender_scene \
    --white_background \
    --eval
```

### 快速原型测试
```bash
# 低分辨率 + 少迭代，快速验证
python train.py -s data/scene -m output/test \
    -r 4 \
    --iterations 7000 \
    --densify_until_iter 5000
```

---

## 参数调优建议

### 提升质量

1. **增加训练时间**
   ```bash
   --iterations 50000
   --densify_until_iter 20000
   ```

2. **降低密集化阈值**（更多高斯）
   ```bash
   --densify_grad_threshold 0.0001
   ```

3. **启用深度监督**
   ```bash
   --depths <depth_folder>
   ```

4. **启用抗锯齿**
   ```bash
   --antialiasing
   ```

### 加速训练

1. **使用稀疏 Adam**
   ```bash
   --optimizer_type sparse_adam
   ```

2. **降低分辨率**
   ```bash
   -r 2  # 或 -r 4
   ```

3. **减少迭代次数**
   ```bash
   --iterations 15000
   --densify_until_iter 10000
   ```

4. **数据放 CPU**（大场景）
   ```bash
   --data_device cpu
   ```

### 节省显存

1. **数据放 CPU**
   ```bash
   --data_device cpu
   ```

2. **降低分辨率**
   ```bash
   -r 2  # 1/2 分辨率
   ```

3. **提高密集化阈值**（更少高斯）
   ```bash
   --densify_grad_threshold 0.0004
   ```

### 处理特殊场景

1. **曝光变化大的场景**
   ```bash
   --train_test_exp
   ```

2. **透明/半透明物体**
   ```bash
   --random_background
   ```

3. **合成数据集**
   ```bash
   --white_background
   ```

4. **高频细节场景**
   ```bash
   --antialiasing
   --densify_grad_threshold 0.0001
   ```

---

## 关键参数解释

### iterations
控制训练总时长。默认 30,000 次对大多数场景足够，复杂场景可增加到 50,000。

### densify_grad_threshold
控制高斯数量的关键参数：
- 越小 → 更多高斯 → 更高质量 → 更慢 → 更多显存
- 越大 → 更少高斯 → 更快 → 质量可能下降
- 默认 0.0002 是良好平衡点

### lambda_dssim
平衡 L1 和 SSIM 损失：
- L1：像素级精确度
- SSIM：感知质量
- 默认 0.2 (80% L1 + 20% SSIM) 适合大多数场景

### position_lr_init/final
位置学习率影响收敛速度和稳定性：
- 初始值太大 → 不稳定
- 初始值太小 → 收敛慢
- 默认值经过充分测试，通常不需要修改

### optimizer_type
- `default`: 标准 Adam，稳定但较慢
- `sparse_adam`: 只优化可见高斯，快 1.5-2 倍，需安装扩展

---

## 常见问题

### Q: 显存不足怎么办？
A: 尝试以下组合：
```bash
--data_device cpu -r 2 --densify_grad_threshold 0.0004
```

### Q: 训练太慢怎么办？
A: 使用稀疏 Adam + 降低分辨率：
```bash
--optimizer_type sparse_adam -r 2
```

### Q: 质量不够好怎么办？
A: 增加训练时间 + 降低密集化阈值：
```bash
--iterations 50000 --densify_grad_threshold 0.0001 --antialiasing
```

### Q: 场景有曝光变化怎么办？
A: 启用曝光补偿：
```bash
--train_test_exp
```

---

## 参考资料

- 论文：[3D Gaussian Splatting for Real-Time Radiance Field Rendering](https://repo-sam.inria.fr/fungraph/3d-gaussian-splatting/)
- 代码：[GitHub Repository](https://github.com/graphdeco-inria/gaussian-splatting)
- 视频教程：[YouTube Tutorial](https://www.youtube.com/watch?v=UXtuigy_wYc)

---

**最后更新**: 2024年10月（包含训练加速、深度正则化、抗锯齿等新特性）
